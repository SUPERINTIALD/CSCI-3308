<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <style>

        table {
            width: 80%;
            margin-top: 1%;
            margin-left: 10%            
        }
        h1{
            margin-left: 10%;
        }
        th, td {
            border: 1px solid black;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }
        .container {
            width: 50%;
            margin-left: 25%;
        }


        button {
            margin-top: 5%;
        }
    </style>
</head>
<body>
    <h1>Courses</h1>
    <table>
        <thead>
            <tr>
                <th>Course ID</th>
                <th>Course Name</th>
                <th>Credit Hours</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {{#each courses}}
            <tr>
                <td>{{ course_id }}</td>
                <td>{{ course_name }}</td>
                <td>{{ credit_hours }}</td>
                <td>
                    {{#if taken}}
                        <button type="button" class="btn btn-primary" disabled>Already took the course</button>
                    {{else}}
                        <form action ='/courses/add' method="POST">
                            <input type="hidden" name="course_id"value="{{course_id}}">
                            <button type="submit" class="btn btn-primary">Add Course</button>
                        </form>
                    {{/if}}
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
    {{> message }}
</body>
</html>
